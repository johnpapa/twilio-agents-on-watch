# The project

This is the running example for **Twilio Agents on Watch** — built up
lesson by lesson starting at the
[repo root](../README.md). It's the same code as the live "Nobody's
Watching the Agent" demo.

If you landed here directly: start at the [root README](../README.md)
instead — it has the actual quick start.
